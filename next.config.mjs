/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["pitplay.in", "res.cloudinary.com"],
  },
  // Add more configurations as needed
};

export default nextConfig;
